# Per-service configuration is set via Railway dashboard variables:
# sehra-api: RAILWAY_DOCKERFILE_PATH=Dockerfile.api
# sehra-web: RAILWAY_DOCKERFILE_PATH=Dockerfile.web
#
# Healthcheck is configured per-service in Railway dashboard settings.
# Do NOT set dockerfilePath here — it overrides all services.
